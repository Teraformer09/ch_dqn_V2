env: telemetry
noise: mixed

obs_dim: 1
num_actions: 2

episodes: 300
max_steps: 100

batch_size: 32
seq_len: 10

gamma: 0.99
learning_rate: 0.001

epsilon_start: 1.0
epsilon_end: 0.05
epsilon_decay: 5000

lambda1: 0.05
lambda2: 0.01
reward_scale: 1.0
train_updates_per_episode: 1
eval_episodes: 20
